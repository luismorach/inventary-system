import { DinamicPipePipe } from './dinamic-pipe.pipe';

describe('DinamicPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DinamicPipePipe();
    expect(pipe).toBeTruthy();
  });
});
