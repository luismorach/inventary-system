const API_URL = 'http://localhost:3000';

export const URLS:string[]=[
    `${API_URL}/login`,
    `${API_URL}/forgot-password`,
    `${API_URL}/change-password`
]
export const URL_AUTH_REFRESH=`${API_URL}/refresh-token`
